import 'package:flutter/material.dart';

class ResponseLoginLogo extends StatelessWidget {
  const ResponseLoginLogo({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      'assets/images/logos/resq-response-team-logo.png',
      height: 110,
    );
  }
}
